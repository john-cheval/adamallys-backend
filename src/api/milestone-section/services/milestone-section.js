'use strict';

/**
 * milestone-section service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::milestone-section.milestone-section');
